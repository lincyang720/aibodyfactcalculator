"use client";
import { useMemo,useState } from "react";import "../tool-pages.css";
export default function PsmfPage(){const [weight,setWeight]=useState(75),[fat,setFat]=useState(20);const lean=useMemo(()=>weight*(1-fat/100),[weight,fat]);const protein=Math.round(lean*2.2);return <div className="tool-page"><Nav/><main className="tool-main"><div className="tool-eyebrow">ADVANCED DIET TOOL</div><h1>PSMF Calculator</h1><p className="tool-lede">Estimate lean body mass and a protein target for a protein-sparing modified fast.</p><div className="calc-layout"><div className="calc-card"><div className="field-grid"><Field label="Weight (kg)"><input type="number" value={weight} onChange={e=>setWeight(+e.target.value)}/></Field><Field label="Body fat (%)"><input type="number" value={fat} onChange={e=>setFat(+e.target.value)}/></Field></div></div><div className="answer-card"><span>DAILY PROTEIN TARGET</span><div className="answer-number">{protein} <small>grams</small></div><p>Estimated lean mass: {lean.toFixed(1)} kg. PSMF is an aggressive short-term protocol and should be supervised by a qualified clinician.</p></div></div><section className="content-block"><h2>Important safety note</h2><p>PSMF is not a standard everyday diet. It can be inappropriate during pregnancy, adolescence, eating-disorder recovery, or with certain medical conditions and medications. Consult a healthcare professional before starting.</p></section><a className="back-cta" href="/">Estimate body fat from a photo →</a></main></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="field"><span>{label}</span>{children}</label>}function Nav(){return <header className="tool-nav"><a href="/">BF · BodyLens</a><div><a href="/tdee-calculator">TDEE</a><a href="/bmi-calculator">BMI</a><a href="/psmf-calculator">PSMF</a></div></header>}
