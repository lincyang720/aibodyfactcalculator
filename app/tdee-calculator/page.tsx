"use client";
import { useMemo, useState } from "react";
import "../tool-pages.css";
export default function TdeePage(){const [sex,setSex]=useState("male"),[age,setAge]=useState(30),[weight,setWeight]=useState(75),[height,setHeight]=useState(178),[activity,setActivity]=useState(1.55);const tdee=useMemo(()=>Math.round(((10*weight)+(6.25*height)-(5*age)+(sex==="male"?5:-161))*activity),[sex,age,weight,height,activity]);return <div className="tool-page"><Nav/><main className="tool-main"><div className="tool-eyebrow">FREE FITNESS TOOL</div><h1>TDEE Calculator</h1><p className="tool-lede">Estimate how many calories you burn each day, including your normal activity and training.</p><div className="calc-layout"><div className="calc-card"><div className="field-grid"><Field label="Sex"><select value={sex} onChange={e=>setSex(e.target.value)}><option value="male">Male</option><option value="female">Female</option></select></Field><Field label="Age"><input type="number" value={age} onChange={e=>setAge(+e.target.value)}/></Field><Field label="Weight (kg)"><input type="number" value={weight} onChange={e=>setWeight(+e.target.value)}/></Field><Field label="Height (cm)"><input type="number" value={height} onChange={e=>setHeight(+e.target.value)}/></Field><Field label="Activity"><select value={activity} onChange={e=>setActivity(+e.target.value)}><option value="1.2">Sedentary</option><option value="1.375">Lightly active</option><option value="1.55">Moderately active</option><option value="1.725">Very active</option></select></Field></div></div><div className="answer-card"><span>ESTIMATED DAILY BURN</span><div className="answer-number">{tdee.toLocaleString()} <small>kcal/day</small></div><p>For gradual fat loss, start near {Math.max(1200,tdee-400).toLocaleString()} kcal. Track your two-week weight trend and adjust—not a single day on the scale.</p></div></div><section className="content-block"><h2>What is TDEE?</h2><p>Total Daily Energy Expenditure combines your resting metabolic needs with movement and exercise. It is a useful starting estimate; real-world intake and weight trends provide the best calibration.</p></section><a className="back-cta" href="/">Analyze body fat from a photo →</a></main></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="field"><span>{label}</span>{children}</label>}function Nav(){return <header className="tool-nav"><a href="/">BF · BodyLens</a><div><a href="/tdee-calculator">TDEE</a><a href="/bmi-calculator">BMI</a><a href="/psmf-calculator">PSMF</a></div></header>}
