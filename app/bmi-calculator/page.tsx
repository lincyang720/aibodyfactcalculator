"use client";
import { useMemo,useState } from "react";import "../tool-pages.css";
export default function BmiPage(){const [weight,setWeight]=useState(75),[height,setHeight]=useState(178);const bmi=useMemo(()=>weight/((height/100)**2),[weight,height]);const level=bmi<18.5?"Underweight":bmi<25?"Healthy range":bmi<30?"Overweight":"Higher range";return <div className="tool-page"><Nav/><main className="tool-main"><div className="tool-eyebrow">FREE HEALTH TOOL</div><h1>BMI Calculator</h1><p className="tool-lede">Calculate your body mass index and see the standard adult reference category.</p><div className="calc-layout"><div className="calc-card"><div className="field-grid"><Field label="Weight (kg)"><input type="number" value={weight} onChange={e=>setWeight(+e.target.value)}/></Field><Field label="Height (cm)"><input type="number" value={height} onChange={e=>setHeight(+e.target.value)}/></Field></div></div><div className="answer-card"><span>YOUR BMI</span><div className="answer-number">{bmi.toFixed(1)}</div><p>{level}. BMI is a screening measure and does not distinguish muscle mass from body fat.</p></div></div><section className="content-block"><h2>Understanding BMI</h2><p>Standard adult categories are underweight below 18.5, healthy from 18.5–24.9, overweight from 25–29.9, and obesity at 30 or above. Athletes may have a higher BMI because of muscle mass.</p></section><a className="back-cta" href="/">Get a visual body-fat estimate →</a></main></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="field"><span>{label}</span>{children}</label>}function Nav(){return <header className="tool-nav"><a href="/">BF · BodyLens</a><div><a href="/tdee-calculator">TDEE</a><a href="/bmi-calculator">BMI</a><a href="/body-fat-percentage-chart">Body Fat Chart</a></div></header>}
